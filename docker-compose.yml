services:
  pypsa-dashboard:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - PYTHONUNBUFFERED=1
    # This allows you to edit code locally and see changes without rebuilding
    volumes:
      - ./dashboard:/app/dashboard