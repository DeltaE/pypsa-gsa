services:
  pypsa-dashboard:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - PYTHONUNBUFFERED=1
    volumes:
      - ./dashboard:/app/dashboard
    env_file:
      - ./dashboard/.env